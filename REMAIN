#include <bits/stdc++.h>
using namespace std;

signed main()
{
	string s; cin >> s;
	int base = 1, ans = 0;
	for(int i = s.length()-1; i > -1; --i){
		ans = (ans + base * int(s[i]-'0')) % 17;
		base = (base * 2) % 17;
	}
	cout << ans;
	return 0;
}
